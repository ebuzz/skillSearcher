{% extends '::base.html.twig' %}

{% block body -%}
<div class="container-fluid">
    <div class="row">
       <div class="col-lg-12">
            <h1 class="page-header">Editar Posición</h1>
        </div>
    </div>
    <form method="POST" action="{{ path('position_update', {'id': position.positionId}) }}"> 
        <div class="col-lg-4">
            <div class="well">
                <div class="row">
                    <div class="col-lg-12">
                        <label>Nombre de cuenta </label>
                        <input type="text" value="{{position.name}}" name="name" class="form-control"/>
                    </div>
                </div>
                </br>
                <div class="row">
                    <span>
                        <a href="{{ path('position') }}">
                            <i class="fa fa-arrow-left"></i> Regresar
                        </a>    
                    </span>
	                <div class="pull-right">
	                    <input type="submit" class="btn btn-sm btn-success" value="Guardar" href="{{ path('position') }}" />
	                </div>
                </div>
            </div>
        </div>
    </form> 
</div>  
{% endblock %}




