{% extends '::base.html.twig' %}

{% block body -%}
<div class="container-fluid">
    <div class="row">
       <div class="col-lg-12">
            <h1 class="page-header">Editar Skills</h1>
        </div>
    </div>
    <form method="POST" action="{{ path('skill_update', {'id': skill.skillId}) }}"> 
        <div class="col-lg-4">
            <div class="well">
                <div class="row">
                    <div class="col-lg-12">
                        <label>Nombre de skill </label>
                        <input type="text" value="{{skill.name}}" name="name" class="form-control"/>
                    </div>
                </div>
                </br>
                <div class="row">
                    <span>
                        <a href="{{ path('skills') }}">
                            <i class="fa fa-arrow-left"></i> Regresar
                        </a>    
                    </span>
	                <div class="pull-right">
	                    <input type="submit" class="btn btn-sm btn-success" value="Guardar" href="{{ path('skills') }}" />
	                </div>
                </div>
            </div>
        </div>
    </form> 
</div>  
{% endblock %}




